window.addEventListener('load', () => {
    let cell = document.querySelector('.cell');
    let desc = document.querySelector('.description');
    cell.classList.add('cell--reveal');
    desc.classList.add('description--reveal');
});